#:kivy 1.10.0
#:import FadeTransition kivy.uix.screenmanager.FadeTransition

Manager:
	transition: FadeTransition()
	TelaListaVoo:

<TelaListaVoo>:
	name: 'voos'

	BoxLayout:
		orientation: 'vertical'
		Label:
			text: 'Lista de Voos'
			size_hint_y: 15
		Button:
			text: 'anteriores'
			font_size: 15
			size_hint_y: 15
			on_release: root.lastMostra()
		BoxLayout:
			size_hint_y: 200
			orientation: 'vertical'
			#passo - atualizar quando aperta o botão
			Label:
				id: label1
				text: str( root.k)
		Button:
			text: 'proximos'
			font_size: 15
			size_hint_y: 15
			on_release: root.nextMostra()